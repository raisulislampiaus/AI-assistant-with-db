{
    "name": "backend",
    "targets": {
        "serve": {
            "executor": "nx:run-commands",
            "options": {
                "command": "uvicorn app:app --reload --host 0.0.0.0 --port 8000",
                "cwd": "apps/backend"
            }
        }
    }
}
